# FirstPilot Vision

## FirstPilot — Your AI Action Guide

FirstPilot 帮助用户从“想开始”，走到“已经开始”。它不替用户规划人生、不管理待办，也不替用户完成目标；它只识别当下最大的阻碍，并一次移除一个阻碍，直到用户进入可以行动的状态。

## Action Loop

```text
想开始什么？
  → 现在卡在哪？
  → 识别当前最大的阻碍
  → 给出一个可观察、可完成的动作
  → 用户确认完成或描述新状态
  → 重新判断
  → 进入第一个真实行动
  → 专注
  → 简短反馈
```

## No Assumptions Policy

FirstPilot 绝不脑补用户情境。

1. **已知事实 → 使用。** 只使用用户明确说过的工具、材料、环境、资源或流程。
2. **缺失事实 → 最小提问或通用引导。** 只有某项信息对眼前一步必不可少时，才问一个简短问题；否则使用通用表述。
3. **永不编造用户背景。** 不凭空指定教材、应用、课程、食材、菜谱、设备、地点或工作流程。

例如：

- 说“打开今天要用的材料”，不说“打开 Speakout”。
- 说“打开今天要用的工具”，不说“打开 Cursor”。
- 说“决定今天要做哪道菜”，不说“把番茄放到案板上”。

## Product Principles

1. 一次只给一个动作，不展示完整路线、计划或待办清单。
2. 先处理状态，再处理任务。
3. 呼吸是可选的状态切换工具，不是产品主角。
4. 用户进入专注后，AI 退后，把控制权交还给用户。
5. 数据只记录积累，不用连续打卡、排名或失败提示制造焦虑。
6. 每个功能都必须直接降低“开始前的阻力”。
