"use client";
import { useMemo, useState } from "react";
import { AppShell } from "./app-shell";
import { useLanguage } from "./language-provider";
import { useStudyData } from "@/hooks/use-study-data";
import { calculateArchive, dayKey, formatFocus } from "@/lib/archive-analytics";
function Metric({ label, value }: { label: string; value: string | number }) { return <article className="rounded-[22px] bg-white p-5 shadow-card"><p className="text-xs font-semibold tracking-[.1em] text-[#7B7B73]">{label}</p><p className="mt-3 font-display text-3xl tracking-[-.05em]">{value}</p></article>; }
export function HistoryPage() { const data = useStudyData(); const { locale, messages: t } = useLanguage(); const [month, setMonth] = useState(() => new Date(new Date().getFullYear(), new Date().getMonth(), 1)); const archive = useMemo(() => calculateArchive(data.history, data.starts, data.breaths, locale), [data.history, data.starts, data.breaths, locale]); if (!data.ready) return null; const h = t.history; const dateLocale = locale === "zh" ? "zh-CN" : "en-US"; const dayMap = new Map(archive.daily.map((x) => [x.day, x])); const days = new Date(month.getFullYear(), month.getMonth() + 1, 0).getDate(); const leading = (month.getDay() + 6) % 7; const records = Array.from({ length: days }, (_, index) => { const day = index + 1; const key = `${month.getFullYear()}-${String(month.getMonth() + 1).padStart(2, "0")}-${String(day).padStart(2, "0")}`; return { day, key, record: dayMap.get(key) }; }); const monthActions = records.reduce((sum, item) => sum + (item.record?.actions ?? 0), 0); const today = dayMap.get(dayKey(new Date())); const todaySessions = data.history.filter((item) => item.completed && dayKey(item.finishedAt) === dayKey(new Date()));
 return <AppShell settings={data.settings}><section className="py-16 sm:py-24"><p className="text-xs font-semibold uppercase tracking-[.14em] text-[#7B7B73]">{h.eyebrow}</p><h1 className="mt-4 font-display text-5xl tracking-[-.06em] sm:text-6xl">{h.title}</h1><p className="mt-5 max-w-2xl text-lg leading-relaxed text-[#6B6B63]">{archive.encouragement}</p><div className="mt-12 grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><Metric label={h.actions} value={archive.actionCount} /><Metric label={h.starts} value={archive.starts} /><Metric label={h.completed} value={archive.completedActions} /><Metric label={h.focus} value={formatFocus(archive.totalFocusSeconds, locale)} /><Metric label={h.breaths} value={`${archive.breathCount} ${h.times}`} /></div><div className="mt-10 grid gap-5 lg:grid-cols-[1.35fr_.85fr]"><article className="rounded-[28px] bg-white p-6 shadow-card"><div className="flex items-center justify-between"><button aria-label={h.previousMonth} onClick={() => setMonth((value) => new Date(value.getFullYear(), value.getMonth() - 1, 1))}>←</button><div className="text-center"><p className="text-sm font-semibold">{h.monthTrace}</p><p className="mt-1 text-xs text-[#77776f]">{month.toLocaleDateString(dateLocale, { year: "numeric", month: "long" })} · {h.actionCount.replace("{count}", String(monthActions))}</p></div><button aria-label={h.nextMonth} onClick={() => setMonth((value) => new Date(value.getFullYear(), value.getMonth() + 1, 1))}>→</button></div><div className="mt-6 grid grid-cols-7 gap-2 text-center text-[10px] text-[#8b8b83]">{h.week.map((day) => <span key={day}>{day}</span>)}</div><div className="mt-2 grid grid-cols-7 gap-2">{Array.from({ length: leading }).map((_, index) => <span key={index} />)}{records.map(({ day, key, record }) => <span key={key} title={h.calendarTitle.replace("{day}", key).replace("{actions}", String(record?.actions ?? 0))} className="flex aspect-square items-center justify-center text-[10px]"><i className={`mr-0.5 h-2 w-2 rounded-full ${!record ? "bg-[#edede8]" : record.actions >= 3 ? "bg-[#4f6331]" : "bg-[#9db66e]"}`} />{day}</span>)}</div><p className="mt-5 text-sm text-[#77776f]">{h.monthSummary.replace("{count}", String(monthActions))}</p></article><article className="rounded-[28px] bg-white p-6 shadow-card"><p className="text-sm font-semibold">{h.today}</p><div className="mt-6 grid gap-3">{[[h.todayStarts, `${today?.starts ?? 0} ${h.times}`], [h.todayCompleted, `${today?.actions ?? 0}`], [h.todayFocus, formatFocus(today?.focusSeconds ?? 0, locale)], [h.todayBreaths, `${today?.breaths ?? 0} ${h.times}`]].map(([label, value]) => <div key={label}><p className="text-xs text-[#77776f]">{label}</p><p className="mt-1 font-display text-3xl">{value}</p></div>)}</div><div className="mt-7 border-t pt-5"><p className="text-xs font-semibold tracking-[.1em] text-[#7B7B73]">{h.todayActions}</p>{todaySessions.length ? todaySessions.slice(0, 3).map((item) => <div key={item.id} className="mt-3"><p className="truncate text-sm font-medium">{item.taskTitle}</p><p className="mt-1 text-xs text-[#77776f]">{h.focusLine.replace("{focus}", formatFocus(item.durationSeconds, locale))}</p></div>) : <p className="mt-3 whitespace-pre-line text-sm text-[#77776f]">{h.noToday}</p>}</div></article></div><section className="mt-12"><p className="text-xs font-semibold uppercase tracking-[.14em] text-[#7B7B73]">{h.fullHistory}</p><div className="mt-5 space-y-3">{archive.daily.length ? archive.daily.map((item) => <article key={item.day} className="flex flex-col gap-2 rounded-[22px] bg-white p-5 shadow-card sm:flex-row sm:justify-between"><h2 className="font-semibold">{new Date(`${item.day}T00:00:00`).toLocaleDateString(dateLocale, { year: "numeric", month: "long", day: "numeric" })}</h2><p className="text-sm text-[#77776f]">{h.historyLine.replace("{actions}", String(item.actions)).replace("{focus}", formatFocus(item.focusSeconds, locale)).replace("{breaths}", String(item.breaths))}</p></article>) : <div className="rounded-[25px] bg-white p-10 text-center text-[#77776f]">{h.empty}</div>}</div></section></section></AppShell>; }
