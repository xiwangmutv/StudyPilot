"use client";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { useEffect } from "react";
import type { Settings } from "@/lib/types";
export function AppShell({children,settings}:{children:React.ReactNode;settings?:Settings}){const path=usePathname();useEffect(()=>{document.documentElement.dataset.theme=settings?.theme??"light"},[settings?.theme]);const links=[['/','首页'],['/history','历史'],['/settings','设置']];return <main className="paper-texture min-h-screen bg-paper px-5 py-5 text-ink transition-colors sm:px-8 lg:px-12"><div className="mx-auto max-w-[1240px]"><nav className="flex items-center justify-between"><Link href="/" className="flex items-center gap-2.5"><span className="grid h-8 w-8 place-items-center rounded-[10px] bg-ink text-sm font-semibold text-white">S</span><span className="text-[17px] font-semibold tracking-[-.04em]">StudyPilot</span></Link><div className="flex items-center gap-1 rounded-full bg-white/70 p-1 text-sm shadow-sm">{links.map(([href,label])=><Link key={href} href={href} className={`rounded-full px-3 py-1.5 transition ${path===href?"bg-ink text-white":"text-[#696960] hover:bg-[#ecece6]"}`}>{label}</Link>)}<span className="ml-2 grid h-8 w-8 place-items-center rounded-full bg-lavender text-[10px] font-bold text-[#4F4875]">{(settings?.name||"X").slice(0,2).toUpperCase()}</span></div></nav>{children}</div></main>}
