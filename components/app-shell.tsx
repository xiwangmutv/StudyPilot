"use client";
import Link from "next/link";
import { useEffect } from "react";
import type { UserProfile } from "@/src/core";
import { useLanguage } from "./language-provider";
export function AppShell({ children, settings, minimal = false }: { children: React.ReactNode; settings?: UserProfile; minimal?: boolean }) { const { locale, messages: t, setLocale } = useLanguage(); useEffect(() => { document.documentElement.dataset.theme = settings?.theme ?? "light"; }, [settings?.theme]); const initials = (settings?.name || "FP").slice(0, 2).toUpperCase(); return <main className="paper-texture min-h-screen bg-paper px-5 py-5 text-ink transition-colors sm:px-8 lg:px-12"><div className="mx-auto max-w-[1240px]"><nav className="flex items-center justify-between gap-4"><Link href="/" className="flex shrink-0 items-center gap-2.5"><span className="grid h-8 w-8 place-items-center rounded-[10px] bg-ink text-sm font-semibold text-white">F</span><span className="text-[17px] font-semibold tracking-[-.04em]">FirstPilot</span></Link>{minimal ? <Avatar settings={settings} initials={initials} size="large" /> : <div className="flex items-center gap-1 overflow-x-auto"><Nav href="/">{t.nav.home}</Nav><Nav href="/breathing">{t.nav.breathing}</Nav><Nav href="/later">{t.nav.later}</Nav><Nav href="/history">{t.nav.history}</Nav><Nav href="/feedback">{t.nav.feedback}</Nav><Nav href="/settings">{t.nav.settings}</Nav><button type="button" onClick={() => setLocale(locale === "zh" ? "en" : "zh")} aria-label={t.switchLanguage} className="ml-1 rounded-full px-2 py-1.5 text-xs text-[#696960] hover:bg-[#ecece6]">{locale === "zh" ? "EN" : "Chinese"}</button><Link href="/settings" aria-label={t.nav.settings} className="ml-1"><Avatar settings={settings} initials={initials} /></Link></div>}</nav>{children}</div></main>; }
function Nav({ href, children }: { href: string; children: React.ReactNode }) { return <Link href={href} className="whitespace-nowrap rounded-full px-3 py-1.5 text-sm text-[#696960] transition hover:bg-[#ecece6]">{children}</Link>; }
function Avatar({ settings, initials, size = "small" }: { settings?: UserProfile; initials: string; size?: "small" | "large" }) { const className = size === "large" ? "h-9 w-9" : "h-8 w-8"; return settings?.avatar ? <img src={settings.avatar} alt={`${settings.name} avatar`} className={`${className} rounded-full object-cover`} /> : <span className={`grid ${className} place-items-center rounded-full bg-lavender text-[10px] font-bold text-[#4F4875]`}>{initials}</span>; }
